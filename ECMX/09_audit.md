---
agent: V
status: complete
date_checked: 2026-09-14
items_count: 48
evidence_count: {papers: 0, patents: 0}
unresolved: 4
---

# 09. 내부 감사 (3차 · 최종 게이트 검증) — ECMX-002

**감사 주체**: 에이전트 V (내부 감사) · **3차 검증일 2026-09-14**
**범위**: 2차 재감사에서 제기한 **재반려 6건**의 해소 여부, **추가 확인 3항목**, **회귀 20축**, `tools/validate.py` **탐지력 재시험**, 게이트 **G2·G4·G5** 재판정.
**권한**: 본 에이전트는 원문 데이터를 수정하지 않았다. 아래는 전부 읽기 전용 검사 결과이며, 산출물은 본 문서 하나다. 도구 탐지력 시험은 원본을 건드리지 않고 사본 트리(`…/scratchpad/vt/`)에 위반을 주입해 수행했다.

> **검증 원칙**: "수정했다"는 주장은 근거로 인정하지 않았다. 9건 전부에 대해 대상 파일을 직접 읽고 python·grep으로 세었으며, 각 항목에 검증 명령과 실측 수치를 함께 남긴다.
>
> **총평**: 재반려 6건 중 **5건 완전 해소 · 1건 부분 해소**다. 2차에서 회귀했던 헤더 `items_count` ↔ 데이터 행수는 **7/7로 복구**되었고, 사유 없는 결측 표지는 9개 CSV **12,924셀 전수 스캔에서 0셀**이다. `validate.py`의 위양성도 사본 시험으로 제거가 실증되었다. 2차 진단이었던 "수정이 데이터 계층에서 멈췄다"는 문제는 이번에 대부분 해소되었다. 다만 **같은 유형의 잔여가 4건** 남았고, 그중 2건은 이번 수정이 건드린 바로 그 문장·그 셀의 이웃이다. 게이트는 **G2·G4·G5 모두 실패**로 판정한다.

---

## 1. 3차 검증 요약표 — 재반려 6건 + 추가 확인 3항목

| 항목 | 적용됐다는 수정 | 검증 명령 | 실측 결과 | 판정 |
|---|---|---|---|---|
| **101** 병합의 본문·헤더 전파 | `04_` 헤더 71 + 본문 "71건(20/27/24)", `05_` 헤더 30 + 본문 "30건(L8 13/L9 17)" + "아래 13건", `05_` 병합 고지 신설 | `sed -n` 헤더 판독 + `csv.DictReader` 층위 집계 + `grep -n "아래 [0-9]*건"` | `04_`:6 = **71**, :30 = "**71건**이며(L5 **20**건, L6 **27**건, L7 **24**건 — 중복 병합 후 최종값)" ↔ `inv_chem.csv` **71행(L5 20/L6 27/L7 24)** 일치. `05_`:6 = **30**, :23 = "총 **30건**(L8 **13**건, L9 **17**건)" ↔ `inv_spec.csv` **30행(L8 13/L9 17)** 일치. `05_`:31 = "아래 **13건**은 이런 '보이지 않는 변수'를…". `05_`:10~15에 병합 고지 신설(E-L8-005/007/008 → D-L6-025/027/026 명기). **지정 5개소 전건 해소.** 다만 `04_`:30 **같은 문장 후단**이 "**74건** 중 실제 제품 품번을 확보한 것은 **46건(62%)**이며, 공급사를 특정한 것은 **68건(92%)**"으로 병합 이전 값을 유지한다 — 실측은 **71건 / 43건(60.6%) / 65건(91.5%)** | **부분 해소** |
| **102** `11_` 본문 ↔ CSV | :289 → 39행, N-025 행 삭제, "N-059~061", "N-073, N-075" | `awk`/`sed` 본문 판독 + E1 표 ID 파싱 후 `essential_items.csv` tier 대조 | :290 = "전체 94행 중 E0은 2행, E1은 **39행**이다" ↔ CSV **E1 39** 일치. E1 표에서 파싱한 **20개 ID 전건이 CSV에서 E1**, 비-E1 혼입 **0건**. **N-025는 E1 표에서 삭제**되고 :389 변경 이력 표로 이동(E1 → E2 하향 사유 기재). "**N-059~061**"(3건 모두 E1), "**N-073, N-075**"(2건 모두 E1). :377·:393의 "E0 2 / E1 39 / E2 44 / E3 9"와도 내부 일치 — 2차에 지적한 문서 내 자기모순 소멸 | **해소** |
| **103** L9 규격 항목의 `필수` | L9 17행 값을 `필수(출하 판정 시험)`·`조건부(규격 항목)`로 분리, `03_`에 운용 규칙 3·4 신설, HTML §9-2 열 이름 변경 + 캡션에 축 구분 | `material_inventory.csv` 층위×`essentiality` 교차집계 + `03_`:135~180 판독 + HTML 표 6 `<td>` 파싱 | `essentiality` 값 분포 = **조건부 81 · 선택 75 · 필수 6 · 필수(출하 판정 시험) 14 · 조건부(규격 항목) 3**. **L9 17행 전건이 괄호 부기형**(필수(출하 판정 시험) 14 / 조건부(규격 항목) 3)이고 L9에 맨 `필수`는 **0행**. `03_`:162~172에 **운용 규칙 3**("L9에서는 `필수`의 뜻이 다르다 — 시험 항목과 판정 기준의 집합")과 **운용 규칙 4**("`필수` 유지 항목에는 근거를 `confidence_reason`에 적는다") 신설. HTML §9-2 표 6 열 이름 = "**출하 판정 필수 여부**"(구 "필수도"), 셀 값 **필수 14 / 조건부 3**으로 CSV와 일치, 캡션에 "**앞 장들의 매트릭스 소재 필수도(E0~E3)와는 다른 축이다 — L9는 물질이 아니라 시험 항목이기 때문이다**" 명시. HTML 표 3 "필수 등재" 합계도 **6**(L5 3 · L8 3 · L9 0)으로 재집계됨 | **해소** |
| **104** 인벤토리 `필수` ↔ M3 상반 | 9행을 `조건부`로 정렬, 남은 순수 `필수` 6행에 유지 근거를 `confidence_reason` 앞부분에 명시 | 9개 ID 직접 판독 + 라미닌 전 SKU 등급 나열 + `필수` 6행 `confidence_reason` 판독 | **C-L1-022 · C-L1-034 · C-L1-035 · D-L6-016 · D-L5-013 · D-L6-021 · D-L6-022 · D-L7-001 · D-L7-005 — 9행 전건 `조건부`**. 라미닌 방향 역전 해소: 라미닌-111(C-L1-014·016) **조건부** ↔ 라미닌-511(C-L1-022) **조건부** — 역전 0건, 라미닌 16개 SKU 중 `필수` **0행**. 순수 `필수` **6행**(D-L5-001/003/004 RGD, E-L8-001 NaCl, E-L8-002 PBS, E-L8-009 NaOH) 전건이 `confidence_reason` **앞부분**에 유지 근거를 갖는다(RGD 3행: "M3에서 E0로 확정된 것은 β1·α6β1 인테그린 매개 접착이라는 기능이며 RGD는 그 자리를 채우는 대표 리간드", 무기 3행: "제거 실험의 대상이 될 수 없는 공정 변수"). **`필수` ↔ M3 E2/E3 상반 0행** | **해소** |
| **105** N-045·N-087 지탱 근거 특정 | 두 행 `confidence_reason`에 전문 확인 DOI와 전문 미확보 DOI를 특정 | `essential_items.csv` 두 행 `confidence_reason` 전문 판독 | N-045: "등급을 지탱하는 근거는 전문을 확인한 신장 오가노이드 동적 공유가교 실험(**DOI:10.1002/advs.202200543**, G′ 0.1 kPa 고정·응력완화 2수준 비교)이며, 전문을 확보하지 못한 **DOI:10.1038/s41563-021-01136-7**은 방향이 같다는 보조 근거로만 사용했다". N-087: "…전문을 확인한 장 오가노이드 HELP 겔 실험(**DOI:10.1002/advs.202004705**…)과 비접착 알지네이트 대조(**DOI:10.1016/j.stemcr.2018.12.001**)이며, 전문 미확보인 **DOI:10.1038/nature20168**은 …등급 근거에서 제외했다". **어느 DOI가 E1을 지탱하고 어느 것이 보조인지 DOI 단위로 특정됨** — 2차에 지적한 "근거 혼합" 비특정 문구 소멸 | **해소** |
| **106** 기준 문서화 검사의 위양성 | 검사 대상을 `01_`~`07_`로 한정, L9 값 검사 규칙 신설 | `tools/validate.py`:335~350 판독 + 사본에 `03_` 기준 절 삭제 주입 | 탐색 대상이 `SRC = [f for f in os.listdir(BASE) if re.match(r"0[1-7]_.*\.md$", f)]`로 한정됨 — `09_audit.md`·`orchestrator_log.md`가 검사 대상에서 제외. **사본에서 `03_`의 기준 절(137~173행)을 통째로 삭제하자 `FAIL [R-13] essentiality 3단계의 판정 기준이 어느 문서에도 정의되지 않음` 출력** (2차에는 동일 주입이 PASS였다). L9 값 규칙도 신설되어 사본에서 `E-L9-001`을 맨 `필수`로 되돌리자 `FAIL [R-13] L9 규격 항목의 essentiality에 매트릭스 필수도 값을 사용` 출력. **지적한 위양성 해소** | **해소** |
| **추가 ①** 잔존 결측 표지 | `US20070224273A1`의 `assignee`·`assignee_type` 2셀을 사유 문자열로 교체 | 해당 행 판독 + 9개 CSV **12,924셀 전수** 결측 표지 스캔(20종 표지, 정확 일치 + 14자 이하 부분 일치) | 2셀 모두 "**assignee 항목을 공보 서지에서 확인하지 못함 — Google Patents·FreePatentsOnline 양쪽 대조에서도 미표시(2026-09-14 확인)**"로 교체 — 확인 경로 2곳 + 날짜 포함. 전수 스캔 결과 **사유 없는 결측 표지 0셀**. 정확 일치 4셀(`essential_items.csv` N-052 `alternatives`="없음", `suppliers.csv` F-009 `pack_size`·`storage`·`lead_time`="해당없음")은 전부 실질 판정이다 — N-052는 "대체재 없음"의 판정이고, F-009는 같은 행 `spec`이 "제품 단종 — 제조사 페이지에 'has been discontinued' 표기"라 해당 없음이 사실이다. 결측 표지로 세지 않는다 | **해소** |
| **추가 ②** 헤더 회귀 | `items_count` ↔ 데이터 행수 7/7 복구 | 7쌍 헤더·행수 자동 대조 | `03_`78↔`inv_bio`78 · `04_`71↔`inv_chem`71 · `05_`30↔`inv_spec`30 · `06_`82↔`suppliers`82 · `07_`116↔`patents_landscape`116 · `11_`94↔`essential_items`94 · `12_`56↔`patent_essentials`56 — **7/7 일치, 복구 확인** | **해소** |
| **추가 ③** `validate.py` 탐지력 재시험 | — | 사본 트리에 16종 주입·무주입 시험(3절) | 기준선 **PASS · 오류 0건**. 16종 중 **10종 탐지 · 6종 미탐지**. 2차의 **위양성 1건은 제거**됨(T1). 미탐지 6종은 전부 "검사 항목 자체가 없는" 구조적 사각이다 | **해소(위양성) / 사각 5종 잔존** |

**해소 집계: 재반려 6건 중 완전 해소 5건 · 부분 해소 1건(101) · 미해소 0건. 추가 확인 3항목 전건 해소.**

---

## 2. 회귀 검사 — 2차까지 통과한 축이 이번 수정으로 깨졌는가

| # | 축 | 2차 실측 | 3차 실측 | 판정 |
|---|---|---|---|---|
| 1 | L1~L9 층위 커버리지 | 9/9, 계 179 | L1 35 · L2 16 · L3 10 · L4 17 · L5 20 · L6 27 · L7 24 · L8 13 · L9 17 = **179**, 규격 외 0 | 유지 |
| 2 | AX-1~AX-9 축 커버리지 | 9/9 | AX-1 32 · AX-2 26 · AX-3 14 · AX-4 15 · AX-5 17 · AX-6 9 · AX-7 9 · AX-8 17 · AX-9 47, 미탐색 **0** | 유지 |
| 3 | 8조직 매트릭스 전 셀 | 80셀 전건 채움 | 8행 × 10열 = **80셀**, 빈 셀 **0**, 규격 외 **0**(E1 23 · E2 33 · E3 24). 8조직 = 장·간·뇌·신장·폐·췌장·위·종양 | 유지 |
| 4 | CSV 공란 0 | 0셀 | 9개 CSV **12,924셀 중 공란 0셀**, 필수필드 결측률 **0.0%** | 유지 |
| 5 | `tier` 값 영역 | 위반 0 | E0 2 · E1 39 · E2 44 · E3 9 = 94, 영역 외 **0** | 유지 |
| 6 | `evidence_type` 값 영역 | 위반 0 | SUBST 48 · DOSE 40 · OMIT 22 · REVIEW 12 · VENDOR 2, 영역 외 **0** | 유지 |
| 7 | `confidence` 값 영역 | 위반 0 | 상 25 · 중 61 · 하 8(M3 기준), 영역 외 **0**. `confidence_reason` 5자 미만 **0건** | 유지 |
| 8 | E0·E1의 강근거 보유 | 41/41 | E0+E1 **41건 전수**가 OMIT·SUBST·DOSE 중 최소 하나 보유, 위반 **0** | 유지 |
| 9 | REVIEW·VENDOR 단독 E0·E1 | 0건 | **0건**(REVIEW 12건 중 E0·E1은 전부 실험 태그 병기) | 유지 |
| 10 | R-10 배지→매트릭스 | `필수` 0행 | `function_role`이 "매트릭스 결합형 아님 / 배지 성분"인 **16행 중 `필수` 0행**. 단 **7행이 `조건부`**로 남아 기준표의 `선택` 정의와 겹친다(4-3절) | **부분 유지** |
| 11 | R-10 2D→3D · 체외→체내 | 위반 0 | 2D 코팅·체내 이식 근거를 쓴 항목 4건 중 **E0·E1은 N-002 1건뿐이고 그 E0는 3D 오가노이드 CRISPR 제거 실험이 지탱**한다. 2D 코팅만으로 선 항목 N-018·N-007은 **E2**, N-023은 **E3** — 2D 근거의 등급 상향 **0건** | 유지 |
| 12 | R-03 유예 표현 | 실질 0건 | 광역 정규식 15종 × 전 `.md` 스캔 → 후보 **39개소**, 판독 결과 전부 기술·서지 서술(조성 미정의, 단종 예정, 만료예정일, 후속 문헌, 계산식 있는 추정치, 전망). **실질 위반 0건** | 유지 |
| 13 | §4 금지 수치 | 무비판 인용 0 | 5개 금지 항목 전 문서 + HTML 스캔 → 검출 **6개소**(`02_`:227, `05_`:257, `08_`:458×3, `08_`:460) **전부 반박·배제 문맥**. 무비판 인용 **0건** | 유지 |
| 14 | 날조 인용 | 0건 | 대장 등재 DOI **364건**. 문서 고유 DOI **355건**·HTML 고유 DOI **361건** — 정규화 후 **미등재 0건** | 유지 |
| 15 | HTML 앵커·외부 자원 | 앵커 113/113, 외부 0 | 외부 자원 참조 **0건**(자체 완결형), 내부 앵커 **113개 전부 대상 존재**, `<figure>` 13 · 인라인 SVG 13 | 유지 |
| 16 | 보고서 수치 ↔ CSV | 전건 일치 | 인벤토리 **179**, 판정 **94**, E0 **2** / E1 **39**, 특허 **116**, 독립항 확인 **85**, 물건항 **40**, 독립항 수치범위 **31/56**, 법적상태 확인 **31/116**, 신뢰도 하 **26/527 = 4.9%** — 전건 CSV 실측 일치. HTML 24개 표의 행수도 캡션 건수와 전건 일치(표 5 = 179+헤더, 표 8 = 82+헤더, 표 A-2 = 527+헤더, 표 B-1 = 365+헤더, 표 B-2 = 127+헤더) | 유지 |
| 17 | 헤더 `items_count` ↔ 행수 | **5/7 (회귀)** | **7/7 일치 — 복구** | **복구** |
| 18 | ID 중복 | 0건 | `material_id` 179개 고유, `essential_id` 94개 고유, 중복 **0** | 유지 |
| 19 | 병합 정합성 | 78+71+30=179 | **78+71+30 = 179**, 통합 ↔ 3분할 차집합 **0**, **필드 단위 불일치 0**, 구 ID(D-L6-019·020, D-L7-023, E-L8-005·007·008) **전건 부재**, 대표 ID 전건 존재 | 유지 |
| 20 | 자기 데이터 서술 정확성 | 6개소 불일치 | `11_`:289·314·320·322 및 `04_`:30 전단·`05_`:14·22 **해소**. **`04_`:30 후단 3수치와 `11_`:299 잔존 = 2개소** | **부분 복구** |

**회귀 0건.** 2차에서 회귀한 1축(#17)은 복구되었다. 이번 수정으로 새로 깨진 축은 없다. #10·#20은 잔존 결함이지 이번 수정이 만든 회귀가 아니다.

---

## 3. `validate.py` 탐지력 재시험

**방법**: 원본을 수정하지 않고 사본 트리를 만들어 위반을 한 종류씩 주입한 뒤 `python3 tools/validate.py <사본>`을 돌려 `FAIL` 행 출력 여부를 확인했다. 기준선(무주입 사본)은 **PASS · 오류 0건 · INFO 34행**이다.

| # | 주입한 위반 | 대응 | 결과 |
|---|---|---|---|
| T1 | `03_materials_bio.md`의 기준 절 전체 삭제 | **재반려-106** | **탐지(신규)** — `FAIL [R-13] essentiality 3단계의 판정 기준이 어느 문서에도 정의되지 않음`. 2차에는 동일 주입이 PASS였다 |
| T2 | `E-L9-001`의 `essentiality`를 맨 `필수`로 되돌림 | 재반려-103 | **탐지(신규)** — `FAIL [R-13] L9 규격 항목의 essentiality에 매트릭스 필수도 값을 사용: E-L9-001 = '필수'` |
| T3 | `essentiality`에 영역 외 값 `매우필수` 주입 | 005 | **탐지** — `FAIL [M2] essentiality 값 오류: C-L1-001 = '매우필수'` |
| T4 | `C-L4-001`(표피성장인자)을 `필수`로 되돌림 | 001 | **탐지** — `FAIL [R-10] 배지·비매트릭스 성분에 '필수' 부여: C-L4-001 표피성장인자` |
| T5 | `patents_landscape.csv`에 맨 `미확인` 주입 | 재반려-107 | **미탐지** — `BAD_MISSING`에 `미확인` 부재 |
| T6 | 같은 셀에 맨 `미상` 주입 | 008 | **탐지** — `FAIL [특허랜드스케이프] 사유 없는 결측 표지 '미상': US4829000A / ipc` |
| T7 | `04_` 헤더 `items_count`를 74로 되돌림 | 재반려-101 | **미탐지** — 검사 항목 자체가 없음(`grep -c items_count tools/validate.py` = **0**) |
| T8 | `C-L1-022`(M3 대응 N-007 E2)를 `필수`로 되돌림 | 재반려-104 | **미탐지** — `essentiality` ↔ `tier` 교차 검사 코드 부재 |
| T9 | `C-L2-005`를 복제해 `D-L6-099`/`L6`로 재등재 | 004 | **탐지** — `FAIL [G2] 동일 물질이 서로 다른 층위에 중복 등재: '메타크릴레이트화히알루론산' → [('L2','C-L2-005'),('L6','D-L6-099')]` |
| T10 | `C-L1-014`만 `필수`로 변경 → SKU 역전 | 005 | **탐지** — `FAIL [R-13] 동일 물질의 제품 간 필수도 불일치: '라미닌111' → ['조건부','필수']` |
| T11 | (무주입) 현존 N-045·N-087 — 초록 전용 E1, `confidence` 중 | 재반려-105 | **미탐지** — `confidence == '하'` AND 조건 때문에 구조적으로 탐지 불가 |
| T11b | `N-025`를 `E1`로 되돌림(`confidence` 하) | 007 | **탐지** — `FAIL [R-13] 초록 전용·신뢰도 하 근거로 E1 부여: N-025` |
| T12 | `11_`:290의 "E1은 39행"을 "41행"으로 되돌림 | 재반려-102 | **미탐지** — 본문 수치 ↔ CSV 대조 검사 부재 |
| T13 | `N-043`을 `E0` + `axis=AX-4` + `tissue_scope=간`으로 변경 | 006 | **탐지** — `FAIL [G4] … 간/AX-4: 항목 N-043 E0 ↔ 매트릭스 전 열 E3`(+ 초록 전용 FAIL 동시 발생) |
| T14 | `material_inventory.csv`에 공란 주입 | 스키마 | **탐지** — `FAIL [M2] 결측 공란: C-L1-004 / evidence_ref — 결측 사유 문자열 필요` |
| T15 | `11_`의 DOI 하나를 대장 미등재 DOI로 치환 | 근거 무결성 | **미탐지** — DOI ↔ `evidence_ledger.md` 대조 검사 부재 |

**결과: 16종 중 10종 탐지(62.5%) · 6종 미탐지. 위양성 0종(2차 1종에서 해소).**

2차 대비 변화는 둘이다. (ㄱ) **T1의 위양성이 제거**되었다 — 기준 절이 없으면 이제 FAIL이 나므로, 도구의 PASS를 기준 존재의 근거로 쓸 수 있다. (ㄴ) **T2 규칙이 신설**되어 L9 값 축 혼용이 자동 탐지된다. 미탐지 6종은 전부 "검사 코드가 아예 없는" 사각이며, 그중 T5·T7·T8·T11은 2차에 이미 지적한 것이 그대로 남았고 T12·T15는 이번에 새로 확인한 사각이다. 특히 **T15(날조 인용)는 본 조사가 근거 무결성의 최종 방어선으로 삼는 축인데 도구에 대응 검사가 없다** — 지금까지의 "날조 0건"은 전부 감사자의 수동 대조로만 확인된 값이다.

---

## 4. 남은 미해결 항목 (4건)

### [잔존-201] `04_materials_chem.md`:30 후단 3개 수치가 병합 이전 값이다 (재반려-101의 잔여)

① **대상**: `04_materials_chem.md`:30 (같은 문장의 후단)

② **실측**: 이 줄 전단은 "등재 물질은 **71건**이며(L5 20건, L6 27건, L7 24건 — 중복 병합 후 최종값)"로 정확히 고쳐졌다. 그런데 **같은 줄 후단**은 고쳐지지 않았다.

| 문서가 쓴 값 | `inv_chem.csv` 실측 |
|---|---|
| "**74건** 중 실제 제품 품번을 확보한 것은 **46건(62%)**" | 모수 **71건**, 실제 품번 보유 **43건(60.6%)** — `catalog_no`가 "맞춤합성…" 또는 "확보 실패:…"로 시작하는 28행 제외 |
| "공급사를 특정한 것은 **68건(92%)**" | **65건(91.5%)** — `supplier`가 "확보 실패:…"인 6행 제외 |

③ **위반 규칙**: G5(산출물 간 상호 정합성), G2(본문 ↔ 데이터 건수 정합), 자기 데이터 서술 정확성.

④ **수정 방법**: "74건 중 … 46건(62%) … 68건(92%)"을 "**71건 중 … 43건(60.6%) … 65건(91.5%)**"으로 고친다. 한 문장 안에서 앞의 모수만 고치고 뒤의 모수를 두었으므로, 병합 전파를 문장 단위가 아니라 **수치 단위**로 다시 훑어야 한다.

---

### [잔존-202] `11_essential_materials.md`:299가 존재하지 않는 매트릭스 E0 셀을 근거로 든다 (반려-006 수정의 미전파)

① **대상**: `11_essential_materials.md`:299

② **실측**: 이 줄은 E0을 2항목으로 좁힌 근거를 설명하면서 이렇게 쓴다 — "…적용 조건이 명시되므로 항목 등급은 E1이고, `essential_matrix.csv`에서는 **해당 조직 셀(간 행의 'AX-1 라미닌/기저막')에만 E0으로 표기했다**."

- `essential_matrix.csv`의 **간 행 / AX-1 라미닌/기저막 셀 값은 `E1`**이며, 그 셀 안에 "**항목 판정 N-003·N-004와 정합하도록 E0→E1 조정, V 감사 반려-006**"이라는 주석까지 들어 있다.
- 매트릭스 **80셀 전수에 `E0` 셀은 0개**다(E1 23 · E2 33 · E3 24).
- 즉 **2차 수정이 셀은 바꾸고 그 셀을 인용한 문장은 두었다.** 재반려-101·102와 동일한 실패 유형이며, 이번에는 방향이 반대다(본문이 데이터보다 강한 등급을 주장).
- 같은 주장은 HTML에는 없다(`grep "에만 E0으로 표기"` HTML = **0건**). 본문 `.md` 1개소 한정이다.

③ **위반 규칙**: R-13(등급 서술 정확성), G4(본문 등급 서술 ↔ CSV), G5.

④ **수정 방법**: 해당 절을 "매트릭스에서는 간 행의 'AX-1 라미닌/기저막'을 **E1**로 두되 셀 단서에 조건 한정 사실을 적었다"로 고친다. 또는 매트릭스 셀을 E0으로 되돌린다 — 다만 그 경우 반려-006에서 확인한 "항목 N-003·N-004가 E1"이라는 판정과 다시 어긋나므로, 본문을 데이터에 맞추는 쪽이 맞다.

---

### [잔존-203] 배지·비매트릭스 7행이 `조건부`로 남아 신설 기준의 `선택` 정의와 겹친다 (반려-001·002의 동종 미해소)

① **대상**: `material_inventory.csv`의 C-L4-004(Wnt3a) · C-L4-005(FGF2) · C-L4-006(FGF7) · C-L4-007(FGF10) · C-L4-010(HGF) · C-L4-014(CHIR99021) · E-L8-016(페니실린-스트렙토마이신)

② **실측**: `03_`:148의 기준표는 `선택`을 "다른 물질로 대체 가능하거나, **매트릭스 성분이 아니어서 매트릭스 필수도를 논할 대상이 아니다**(예: 배지 첨가형 성장인자)"로 정의한다. 그런데 `function_role`이 "**배지 첨가형(medium supplement) — 매트릭스 결합형 아님**"인 16행이 두 등급으로 갈린다.

| 등급 | 행수 | ID |
|---|---|---|
| `선택` | **9** | C-L4-001 EGF · C-L4-002 노긴 · C-L4-003 R-spondin-1 · C-L4-008 BMP4 · C-L4-009 TGF-β1 · C-L4-011 VEGF165 · C-L4-012 Y-27632 · C-L4-013 A83-01 · E-L8-004 중탄산나트륨 |
| `조건부` | **7** | C-L4-004 Wnt3a · C-L4-005 FGF2 · C-L4-006 FGF7 · C-L4-007 FGF10 · C-L4-010 HGF · C-L4-014 CHIR99021 · E-L8-016 Pen-Strep |

그중 **C-L4-004(Wnt3a)는 명시적 매핑 위반**이다. 기준표는 `조건부` ↔ "E1(조건 한정) 및 E2", `선택` ↔ "E2·E3, 또는 매트릭스 비해당"으로 매핑하는데, Wnt3a의 대응 M3 항목은 **N-067 "배지 성장인자(EGF/Noggin/R-spondin/Wnt3a)" `tier` = E3**다. 같은 M3 한 행이 묶은 4개 물질 중 **EGF·노긴·R-spondin 3개는 반려-001로 `선택`이 되었고 Wnt3a만 `조건부`로 남았다.** 나머지 6행은 대응 M3 항목이 없어 매핑표가 직접 구속하지 않으나, `선택` 정의의 후단("매트릭스 성분이 아니어서 논할 대상이 아니다")에는 전건 해당한다.

③ **위반 규칙**: R-10(배지 ↔ 매트릭스 구분), R-13, G2(등급 부여 근거의 일관 적용), G5(동일 물질 필수도 일치 · 등급 체계 간 정합).

④ **수정 방법**: 7행을 `선택`으로 내린다. 최소한 **C-L4-004는 N-067 E3 매핑과 동일 M3 행의 3개 형제 물질에 맞춰 `선택`**이어야 한다. 내리지 않는다면 `03_`의 기준표에 "배지 성분이라도 조직 특이적 요구가 확인되면 `조건부`를 쓴다"는 규칙을 명시하고 각 행 `confidence_reason`에 그 근거를 적는다 — 운용 규칙 4가 `필수`에 대해 요구한 것과 같은 형식이다.

---

### [잔존-204] `validate.py`의 구조적 사각 5종 (재반려-106의 잔여)

① **대상**: `tools/validate.py`

② **실측**: 3절의 주입 시험에서 미탐지 6종이 나왔고, 원인은 전부 "검사 코드 부재"다.

| 사각 | 근거 | 놓치는 결함 |
|---|---|---|
| `BAD_MISSING`에 `미확인`·`확인불가` 부재 | :256 `BAD_MISSING = ("unknown","미상","n/a","na","-","?","미정")` | T5 — 재반려-107이 지적했던 2셀 유형 |
| 헤더 `items_count` ↔ 데이터 행수 검사 부재 | `grep -c items_count tools/validate.py` = **0** | T7 — 재반려-101, 2차 회귀의 원인 |
| `essentiality` ↔ `tier` 교차 검사 부재 | 코드에 해당 블록 없음 | T8 — 재반려-104, 잔존-203 |
| 초록 전용 검사의 `confidence == '하'` AND 조건 | :399 | T11 — 재반려-105의 N-045·N-087(둘 다 `중`) |
| 본문 수치 ↔ CSV 대조, DOI ↔ 대장 대조 부재 | 코드에 해당 블록 없음 | T12·T15 — 재반려-102 유형, 그리고 **날조 인용** |

③ **위반 규칙**: 검사 체계 신뢰성, G5.

④ **수정 방법**: (ㄱ) `BAD_MISSING`에 `미확인`·`확인불가`를 추가한다. (ㄴ) 각 `.md` 헤더의 `items_count`를 대응 CSV 행수와 대조하는 검사를 신설한다. (ㄷ) `essentiality`(필수/조건부/선택) ↔ `essential_items.tier`(E0~E3) 매핑 검사를 신설한다. (ㄹ) 초록 전용 검사에서 `confidence == '하'` AND 조건을 제거하고 `tier in (E0,E1)` AND "본문 미확인"만으로 판정한다. (ㅁ) 문서·HTML의 전 DOI를 `evidence_ledger.md`와 대조하는 검사를 신설한다 — **탐지력 개선 중 가장 우선순위가 높다**. (ㅂ) `11_`·`04_`·`05_` 본문의 "N건" 패턴을 대응 CSV 집계와 대조하는 검사를 신설하면 T12형을 잡는다.

---

### 4-5. 재반려에 이르지 않는 경미 사항 3건

- **`03_materials_bio.md`:150**이 "**두 가지 운용 규칙을 둔다.**"라고 쓰고 그 아래에 규칙을 **4개** 나열한다. 규칙 3·4가 재반려-103 수정으로 추가되면서 도입 문장이 남았다. 내용 오류는 아니다.
- **`figures/figure_specs.md`:88**에 합계 **185**, **`10_confidence.md`:221**에 "물질 인벤토리 (**185건**)", **`orchestrator_log.md`:38**에 "총 **185건** ≥ MIN_MATERIALS(80)"이 남아 있다. `10_`:12~18이 "본 판정은 감사 반영 **이전** 스냅숏"임을 고지하고 `orchestrator_log.md`:110이 병합을 기록하므로 오인 경로는 차단되어 있다. 세 파일 모두 본 검증의 지정 대상(`01_`~`07_`, `11_`, `12_`)이 아니다.
- **`essential_items.csv`의 N-045·N-087 `confidence_reason`이 " / "로 시작**한다. 재반려-105 수정으로 DOI 특정 문구를 덧붙이면서 앞의 원 사유 문자열이 비워졌다. 내용은 완결되어 있고 5자 미만 결측 검사에도 걸리지 않지만, 다른 92행의 "원사유 / 추가사유" 형식과 모양이 다르다. **HTML §6-2** 셋째 문단의 "매트릭스 **매트릭스에서**" 어구 중복도 2차와 동일하게 남아 있다.

---

## 5. 게이트 최종 판정

### G2 — M2 물질 인벤토리 · **실패** (1차 실패 → 2차 실패 → 유지)

| 기준 | 실측 | 판정 |
|---|---|---|
| L1~L9 전 층위 등재 | 9/9, 최소 층위 L3 = 10건 | 충족 |
| 최소 물질 수 80건 | **179건 (224%)** | 충족 |
| 필수필드 결측률 < 20% | **0.0%** (12,924셀 중 공란 0) | 충족 |
| `material_id` 중복 0 | **0건** (179개 고유) | 충족 |
| L1·L6·L7 공급사+품번 보유율 ≥ 60% | **97.7% (84/86)** | 충족 |
| 물질 단위 무결성(동일 물질 1행) | 층위 간 중복 **0군**, 구 ID 6개 전건 부재, 3분할 ↔ 통합 필드 불일치 0 | 충족 |
| 역할 기재 정확성(R-10) — `필수` | 배지·비매트릭스 16행 중 `필수` **0행** | 충족 |
| 결측 표지 규칙 | 사유 없는 결측 표지 **2셀 → 0셀** (전 CSV 12,924셀 전수) | **충족(해소)** |
| **등급 부여 근거 존재·일관 적용** | 기준 절 + 운용 규칙 4개 신설. `필수` **29행 → 6행**, 6행 전건 유지 근거 기재. L9 17행 축 분리 표기. `필수` ↔ M3 E2/E3 상반 **0행**. **그러나 배지·비매트릭스 7행이 `조건부`로 남아 기준표의 `선택` 정의와 겹치고, 그중 C-L4-004는 대응 M3 항목 N-067(E3)의 매핑을 정면으로 위반** | **미충족([잔존-203])** |
| **본문 ↔ 데이터 건수 정합** | `04_`:6·30 전단, `05_`:6·14·22 해소. 헤더 `items_count` **7/7 복구**. **`04_`:30 후단 3수치(74 / 46·62% / 68·92%)가 병합 이전 값** | **미충족([잔존-201])** |

**판정: 실패.** 10개 기준 중 **8개 충족 · 2개 미충족**이다. 2차의 미충족 3개(103·104·107) 중 **2개는 완전 해소**되었고 1개(결측 표지)도 해소되어, 실질적으로 **미충족 3개 → 2개**로 줄었다. 정량 기준 5개는 전부 여유 있게 충족한다. 남은 2개는 **모수 3개와 등급 7행**이라는 좁은 범위지만, 둘 다 **"기준·병합을 정확히 적용한 바로 그 자리의 이웃을 빠뜨렸다"**는 같은 성격이다 — 한 문장 안에서 앞 모수만 고치고 뒤 모수를 두었고, 명명된 6행은 내리고 같은 M3 행의 네 번째 물질은 두었다. 기계적 전수 대조 없이 지적된 항목만 따라간 수정 방식이 남긴 잔여다.

### G4 — M3 필수물질 판정 · **실패** (1차 조건부 통과 → 2차 실패 → 유지)

| 기준 | 실측 | 판정 |
|---|---|---|
| AX-1~AX-9 전 축 탐색 | 9/9 (최소 AX-6·AX-7 각 9건) | 충족 |
| ORGANOID_TYPES 8조직 행 | 8/8, **80셀 전건 채움**, 빈 셀 0 | 충족 |
| 매트릭스 셀 값 E0~E3 규격 | 규격 외 **0건** (E1 23 · E2 33 · E3 24) | 충족 |
| E0·E1의 OMIT/SUBST/DOSE 보유 | **41/41 보유**, 위반 **0건** | 충족 |
| REVIEW/VENDOR 단독 E0·E1 | **0건** | 충족 |
| `tier`·`evidence_type`·`confidence` 값 영역 | 위반 **0건** | 충족 |
| `confidence_reason` 충실성 | 5자 미만 **0건** | 충족 |
| 항목 등급 ↔ 매트릭스 셀 일치 | 최대 모순 **0건**, 반려-006 지정 3셀 수정 유지 | 충족 |
| **E0·E1 근거의 실질성** | 초록 전용 E1 2건(N-045·N-087)에 **지탱 DOI와 보조 DOI가 각각 특정**됨. 1차 수정 방법 ④의 문면이 이행됨 | **충족(해소)** |
| **본문(`11_`) 등급 서술 ↔ CSV** | 지적 4개소(:289·314·320·322) **전건 해소**, 문서 내부 자기모순 소멸. **`11_`:299가 "매트릭스 간 행 AX-1에만 E0으로 표기"라고 쓰는데 해당 셀은 E1이고 매트릭스 80셀에 E0은 0개** | **미충족([잔존-202])** |

**판정: 실패.** 10개 기준 중 **9개 충족 · 1개 미충족**이다. 2차에 G4를 실패시킨 두 근거 중 **재반려-105는 완전히 해소**되었고 **재반려-102의 지정 4개소도 전건 해소**되었다. 미충족으로 남은 것은 **문장 하나**다. 그러나 그 문장은 "E0을 2항목으로 좁힌 이유"를 설명하는 자리에서 **존재하지 않는 E0 셀을 근거로 제시**하므로, 판정 문서의 핵심 논증이 자기 데이터보다 강한 주장을 하고 있다. 기준의 성격상 부분 충족을 인정할 수 없어 미충족으로 둔다. **이 한 문장을 데이터에 맞추면 G4의 10개 기준이 전부 충족된다.**

### G5 — 산출물 간 상호 정합성 · **실패** (1차 실패 → 2차 실패 → 유지)

> G5의 정의는 1·2차와 동일하게 "12개 산출물이 동일 사실에 대해 상충하는 값을 발행하지 않을 것"으로 둔다.

| 기준 | 실측 | 판정 |
|---|---|---|
| 유예 표현(R-03) 0건 | 후보 39개소 전부 기술·서지·전망 서술, 실질 위반 **0건** | 충족 |
| §4 금지 수치 무비판 인용 0건 | 검출 6개소 **전부 반박·배제 문맥** | 충족 |
| 날조 인용 0건 | 대장 364건, 문서 고유 DOI 355 · HTML 361 — **미등재 0건** | 충족 |
| 병합 정합성 | 78+71+30 = 179, 차집합 0, **필드 단위 불일치 0**, 구 ID 전건 부재 | 충족 |
| R-10 2D↔3D · 체외↔체내 | 2D·체내 근거 항목 4건 중 E0·E1은 1건이고 그 E0는 3D 제거 실험이 지탱. 2D 근거의 등급 상향 **0건** | 충족 |
| R-10 배지↔매트릭스 (`필수` 축) | 배지·비매트릭스 16행 중 `필수` **0행**, HTML 부록 A에도 사유 반영 | 충족 |
| `11_`↔`12_` 필수성 개념 분리 | `CLAIM` 등급 0건, `patent_ref` 94/94행 사유 문자열 | 충족 |
| HTML 집계 ↔ CSV | 179 / 2 / 39 / 44 / 9 / 94 / 116 / 85 / 40 / 31·56 / 31·116 / 527·26·4.9% — **전건 일치**. HTML 24개 표의 행수도 캡션과 전건 일치 | 충족 |
| 판정 파일 간 정합(tier ↔ 매트릭스) | 최대 모순 0건, 셀 단서에 조정 사유 기재 | 충족 |
| 헤더 `items_count` ↔ 데이터 행수 | **7/7** (2차 5/7에서 복구) | **충족(복구)** |
| 결측 표지 규칙 | 전 CSV 12,924셀 전수, 사유 없는 결측 표지 **0셀** | **충족(해소)** |
| **자기 데이터 서술 정확성** | `11_`:289·314·320·322, `04_`:30 전단, `05_`:14·22 해소(6개소 → 0). **`04_`:30 후단 3수치, `11_`:299 = 2개소 잔존** | **미충족([잔존-201]·[202])** |
| **동일 물질 필수도 일치** | 라미닌 방향 역전 해소(16개 SKU에 `필수` 0행), `필수` ↔ M3 E2/E3 상반 0행. **동일 M3 항목 N-067(E3)이 묶은 4개 물질이 `선택` 3 / `조건부` 1로 갈림** | **미충족([잔존-203])** |
| **등급 체계 간 정합** | L9 축 분리 + 운용 규칙 3·4 신설 + HTML 열 이름·캡션 분리로 **`필수` 29행 중 18행 위반 → 7행**. 매핑표 위반은 **1행**(C-L4-004), `선택` 정의 포섭은 **7행** | **미충족([잔존-203])** |
| **검사 체계 신뢰성** | 주입 16종 중 10종 탐지, **위양성 1 → 0**(해소). **구조적 사각 5종 잔존**, 그중 DOI ↔ 대장 대조 부재는 근거 무결성 축에 대한 자동 검증이 전무하다는 뜻 | **미충족([잔존-204])** |

**판정: 실패.** 15개 기준 중 **11개 충족 · 4개 미충족**이다. 2차의 6개 미충족 중 **헤더 `items_count`·결측 표지 2개는 완전 해소**되었고, 나머지 4개는 **범위가 크게 줄었으나 0이 되지 못했다**(자기 데이터 서술 6개소 → 2개소, 등급 체계 위반 18행 → 7행, 필수도 상반 5행 → 1행, 도구 위양성 1 → 0·사각 4 → 5).

**3차 실패의 핵심도 단일하다 — 수정이 "지적된 목록"을 따라가고 "그 목록의 정의역 전체"를 훑지 않았다.** 재반려 6건에 명시적으로 적힌 **대상 위치와 대상 ID는 하나도 빠짐없이 정확히 처리**되었다(5개소·4개소·17행·9행·2행·2개 규칙). 빠진 것은 전부 **명시되지 않았지만 같은 규칙이 적용되는 이웃**이다 — 같은 문장의 뒤쪽 모수, 같은 M3 행의 네 번째 물질, 고친 셀을 인용한 다른 문서의 문장. 이 유형은 사람이 문장을 따라 고치는 한 반복되므로, [잔존-204]의 자동 검사 5종(특히 `items_count` 대조, `essentiality`↔`tier` 교차, 본문 수치 ↔ CSV 대조, DOI ↔ 대장 대조)을 먼저 넣고 그 출력으로 남은 3건을 정리하는 순서가 맞다.

정정 대상의 총량은 **본문 2개 문장 · CSV 7행 · `validate.py` 5개 규칙**이다. 산출물의 구조나 근거 기반을 다시 만들 필요는 없다. 근거 무결성(날조 인용 0건, DOI 355·361건 전건 대장 등재)과 정량 게이트(물질 179건, 판정 94건, 결측률 0.0%, 보유율 97.7%, 80셀·41건 전수)는 1차 이후 한 번도 흔들리지 않았다.

**게이트 최종 판정: G2 실패 · G4 실패 · G5 실패.**

---

## 6. 감사 이력

### 6-1. 1차 감사 (요약)

**1차 감사일 2026-09-14 · 검사 28항목 중 통과 19 · 반려 9 · 게이트 G2 실패 · G4 조건부 통과 · G5 실패.** 통과 19항목은 누락 축(L1~L9 185건, AX-1~AX-9, 8조직 80셀, M1 O1~O8, YAML 헤더 12/12), 스키마 축(CSV 공란 0, `confidence`·`tier`·`evidence_type` 값 영역 위반 0, ID 중복 0), 규칙 축(R-10 2D↔3D·체외↔체내 위반 0, E0·E1 강근거 43/43, REVIEW/VENDOR 단독 E0·E1 0, R-03 실질 위반 0, §4 무비판 인용 0), 근거 축(고유 DOI 364건 전건 대장 등재, 병합 정합 78+74+33=185, 헤더 `items_count` 7/7)이었다. 반려 9건은 ①배지·공정 성분 6행에 매트릭스 `필수` 부여(R-10) ②동일 물질 필수도가 4개 산출물 간 상반 ③`11_`:60의 `patent_ref` 오기술 ④중복 등재 6군 ⑤`essentiality` 3단계 판정 기준이 12개 문서 어디에도 미정의(grep 0건) + SKU별 등급 상충 8군 ⑥항목 등급 ↔ 매트릭스 3셀 불일치 ⑦초록 전용 근거로 E1 부여 4건 ⑧사유 없는 `미상` 3셀 ⑨`validate.py`가 위 8건을 전부 미탐지였다. 진단의 요지는 "하니스가 R-10을 가장 정확히 서술하는 여러 곳을 두고도 자기 인벤토리에서 그 규칙을 어겼다"였다.

### 6-2. 2차 재감사 (요약)

**2차 재감사일 2026-09-14 · 검사 37항목 · 1차 반려 9건 + 레드팀 지적 3건 = 12건 중 완전 이행 6 · 부분 이행 6 · 미이행 0 · 재반려 6건 · 게이트 G2·G4·G5 전부 실패.** CSV 계층의 수정은 정확했다 — 배지 6행 `선택` 하향, 중복 6군 병합(185 → 179), `미상` 3셀 사유 문자열 교체, N-025·N-043 E2 하향, 총설 6건 `REVIEW` 태그 추가(6 → 12건)와 그중 5건 신뢰도 하향, 매트릭스 3셀 수정, HTML 전 수치 재생성이 전건 확인되었다. 그러나 **병합과 하향이 본문·헤더로 전파되지 않아 1차에 통과했던 헤더 `items_count` 7/7이 5/7로 회귀**했고, 신설된 `essentiality` 판정 기준이 자기 데이터 `필수` 29행 중 18행에서 지켜지지 않았다. 재반려 6건은 **101**(병합 미전파 5개소) **102**(`11_` 본문 4개소 ↔ CSV 불일치) **103**(L9 규격 항목 14행의 `필수`가 신설 기준 위반) **104**(인벤토리 `필수` ↔ M3 E2/E3 상반 5행 + 점검 대상 5행, 라미닌 방향 역전) **105**(N-045·N-087 지탱 근거 미특정) **106**(`validate.py` 기준 문서화 검사가 `09_audit.md` 자신으로 충족되는 위양성 + 구조적 사각 4종)이었다. 진단의 요지는 "**수정이 데이터 계층에서 멈췄다** — 전파되지 않은 곳은 사람이 손으로 쓴 산문이다"였고, 3차 검증 결과 그 지적은 대부분 해소되었다.

---

## 7. 3차 검증 종료 선언

검증 항목 48건(재반려 6 · 추가 확인 3 · 회귀 20축 · 도구 주입 시험 16 · 게이트 3)을 검사했다. **재반려 6건 중 완전 해소 5건 · 부분 해소 1건 · 미해소 0건**, **추가 확인 3항목 전건 해소**, **회귀 0건**(2차 회귀 1축은 복구), **신규·잔존 미해결 4건**(경미 3건 별도).

본 감사는 원문 데이터를 수정하지 않았다. `validate.py` 탐지력 시험은 전부 사본 트리에서 수행했고 원본 도구와 데이터는 변경되지 않았다. 기준을 낮춰 통과시킨 게이트는 없다.

**게이트 최종 판정: G2 실패 · G4 실패 · G5 실패.**
