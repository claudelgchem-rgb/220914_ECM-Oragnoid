---
agent: V
status: complete
date_checked: 2026-09-14
items_count: 37
evidence_count: {papers: 0, patents: 0}
unresolved: 6
---

# 09. 내부 감사 (2차 · 재감사) — ECMX-002 수정 적용 검증

**감사 주체**: 에이전트 V (내부 감사) · **재감사일 2026-09-14**
**대상**: 본문 `01_`~`07_`·`11_`·`12_`, 데이터 CSV 9종, `ECMX_report.html`, `tools/validate.py`
**권한**: 본 에이전트는 원문 데이터를 수정하지 않았다. 아래는 전부 읽기 전용 검사 결과이며, 산출물은 본 문서 하나다.

> **재감사 원칙**: "수정했다"는 주장은 근거로 인정하지 않았다. 12건 전부에 대해 대상 파일을 직접 읽고 python·grep으로 세었으며, 각 항목에 검증 명령과 실측 수치를 함께 남긴다. `tools/validate.py`에 대해서는 원본을 건드리지 않고 `/tmp` 사본에 위반을 **주입**해 탐지 여부를 시험했다.
>
> **총평**: 12건 중 **6건은 완전 이행**, **6건은 부분 이행**이다. 전혀 손대지 않은 건은 0건이다. 그러나 병합·하향이 CSV에만 적용되고 본문·헤더로 전파되지 않아 **1차에서 통과했던 "헤더 `items_count` ↔ 데이터 행수 7/7 일치"가 5/7로 회귀**했고, 새로 정의된 `essentiality` 판정 기준이 자기 데이터 29행 중 18행에서 지켜지지 않는다. **재반려 6건**을 아래에 기재한다.

---

## 1. 재감사 요약표 — 1차 반려 9건 + 레드팀 지적 3건

| 반려 | 요구한 수정 | 검증 명령 | 실측 결과 | 판정 |
|---|---|---|---|---|
| **001** | 배지·공정 성분 6건 `필수` → `선택` (R-10) | `material_inventory.csv` 6개 ID의 `essentiality` 직접 판독 | C-L4-001/002/003/012/013, E-L8-004 **전 6행 `선택`**. `function_role`에 "매트릭스 결합형 아님" 유지. `confidence_reason`에 "(V 감사 반려-001 반영)" 기재. HTML 부록 A에도 동일 사유 문자열 출력 | **이행 확인** |
| **002** | 동일 물질 필수도 상반 해소 | 인벤토리 ↔ `essential_items.csv` ↔ `essential_matrix.csv` 3자 대조 | 배지 성장인자: 인벤토리 `선택` / N-067 **E3** / 매트릭스 AX-6 8조직 중 **7칸 E3**(폐만 E2, 이는 N-071 자가분비 ECM 건으로 별개 물질) — **원건 6건 정합화 확인**. 그러나 **동종 상반 5행 잔존**(3-8절) | **부분 이행** |
| **003** | `11_`:60 자기 데이터 오기술 재작성 | `awk 'NR==60'` + `patent_ref` 열 전수 집계 | 60행이 "`patent_ref` 열에는 전 행에 그 사실을 명시하는 문자열을 기재했다"로 재작성됨. CSV 실측: `patent_ref` **94/94행이 동일 사유 문자열**, 공란 0, `evidence_type`에 `CLAIM` **0건** — 서술과 데이터 일치 | **이행 확인** |
| **004** | 중복 6군 병합, 185 → 179 | 행수·ID 차집합·필드 대조 | `material_inventory.csv` **179행**, 제거 대상 6개 ID **전건 부재**. `inv_bio 78 + inv_chem 71 + inv_spec 30 = 179`, 차집합 0, **필드 단위 불일치 0**. 대표 행 `function_role`에 "(중복 등재 통합, 구 ID …)" 주석. **다만 본문·헤더 5개소 미반영**(3-1·3-2절) | **부분 이행** |
| **005** | `essentiality` 판정 기준 절 신설 + 등급 역전 해소 | `03_materials_bio.md`:137~161 판독 + 물질명 정규화 군집 | 3단계 기준표(필수/조건부/선택 ↔ E0~E3 대응) + 운용 규칙 2개 신설 확인. SKU 군집 6군(콜라겐IV형·라미닌111·기저막추출물·알지네이트·키토산·mTG) **등급 역전 0건**. 그러나 **신설 기준이 `필수` 29행 중 18행에서 미적용**(3-7·3-8절) | **부분 이행** |
| **006** | 매트릭스 3개 셀 수정 | `essential_matrix.csv` 셀 직접 판독 + `axis` 열 검사 | 간/AX-1 = **E1**("항목 판정 N-003·N-004와 정합하도록 E0→E1 조정, V 감사 반려-006" 명기). 장/AX-4 셀에 "계대를 위한 회수 경로로서의 분해성은 별도 요건으로 E1(N-056)" 단서 추가. N-094 `axis` = `AX-9;AX-2;AX-8` — **AX-1 제거 확인**. 80셀 전건 E1~E3, 규격 외 0 | **이행 확인** |
| **007** | N-025·N-043 E1 → E2, 사유 기재 | `tier`·`open_issues`·`confidence_reason` 판독 | N-025·N-043 **둘 다 E2**. `open_issues`에 "…E1에서 E2로 하향(V 감사 반려-007). 전문 확보 시 E1 재검토 대상." 기재. **그러나 N-045·N-087은 여전히 E1 + `experiment_summary`에 "본문 미확인"**이고, 수정 지시 ④가 요구한 "어느 근거가 등급을 지탱하는지 명시"가 **"근거 혼합 — 일부는 전문에서 …, 일부는 초록 범위에 그침"**이라는 비특정 문구로 대체됨 | **부분 이행** |
| **008** | `미상` 3셀 → 사유 문자열 | `patents_landscape.csv` 3셀 직접 판독 + 전 CSV 결측 표지 스캔 | WO2026047653A2 `ipc` = "공보 서지에 국제특허분류 미수록 — Google Patents 재대조에서도 미표시(2026-09-14 확인)", KR102965763B1·US20260137836A1 `priority_date` = "공보 서지에 우선일 미표시 — 출원일만 공개된 건(2026-09-14 확인)" — **지정 3셀 해소**. 그러나 **US20070224273A1의 `assignee`·`assignee_type` 2셀이 사유 없는 `미확인`**으로 잔존 | **부분 이행** |
| **009** | 7개 결손 항목 추가 | 도구 실행 + `/tmp` 사본에 위반 12종 주입 | `check_extra()` 신설(가~바 6블록, 7항목). 주입 시험 **12종 중 8종 탐지**. **위양성 1건**(기준 문서화 검사가 감사문 자체로 충족됨) + **구조적 사각 4종**(4절) | **부분 이행** |
| **X-001** | HTML E0 집계 ↔ CSV 일치 | HTML 텍스트 추출 후 전 수치 대조 | HTML 헤더 "**절대필수(E0) 2건 · 조건부필수(E1) 39건**", 그림 1 "E0 2건 / E1 39건 / E2 44건 / E3 9건", 표 10 E0 2행 — **CSV(E0 2·E1 39·E2 44·E3 9)와 전건 일치** | **이행 확인** |
| **X-007** | 총설 6건 `REVIEW` 태그 + 신뢰도 하향 | `evidence_type`·`confidence`·`evidence_ref` 참조수 집계 | N-050·N-058·N-062·N-064·N-093·N-094 **전 6건에 `REVIEW` 추가**(총 6 → **12건**, HTML "REVIEW 12건"과 일치). 이 중 독립 원저 1편인 5건(N-050·058·062·064·093)이 **신뢰도 `중`**, 원저 3편인 N-094만 `상` 유지 — 규칙과 데이터 일치 | **이행 확인** |
| **X-002~005·008** | HTML 요약·§6-2·§7-1 자기 데이터 정합 | 5대 발견·§6-2 3개 주장·§7-1 수치 전건 대조 | 발견①: E0 2건 모두 인테그린 매개 접착 ✓. 발견②: AX-2 26건(E1 14/E2 12)·AX-3 14건(E1 8/E2 6) — **E0 0건**으로 "절대 필수 미도달" ✓. 발견④: 간 12~38 Pa ↔ 1.3~1.7 kPa 상충 명시 ✓. 발견⑤·§7-1: US4829000A 만료 2006-05-09, EP0218065B1 2006-08-22 ✓. §6-2③ "성장인자 행은 거의 모든 조직에서 E3" = 8조직 중 7 ✓. §7-1 물건항 **40건**·독립항 수치범위 **31건**·독립항 확인 **85건**·법적상태 확인 **31/116** — 전건 CSV 실측 일치. 신뢰도 하 **26/527 = 4.9%** 정확 | **이행 확인** |

**수정 확인 집계: 12건 중 완전 이행 6건, 부분 이행 6건, 미이행 0건.**

---

## 2. 회귀 검사 — 1차 통과 항목이 여전히 통과하는가

| # | 항목 | 1차 | 2차 실측 | 판정 |
|---|---|---|---|---|
| 1 | L1~L9 층위 커버리지 | 9/9, 계 185 | L1=35 L2=16 L3=10 L4=17 L5=20 L6=27 L7=24 L8=13 L9=17, **계 179**, 규격 외 0 | 유지 |
| 2 | AX-1~AX-9 축 커버리지 | 9/9 | AX-1=32 AX-2=26 AX-3=14 AX-4=15 AX-5=17 AX-6=9 AX-7=9 AX-8=17 AX-9=47, 미탐색 0 | 유지 |
| 3 | ORGANOID_TYPES 8조직 | 8/8, 80셀 | 8행 × 10열, **80셀 전건 E1~E3**(E1 23·E2 33·E3 24), 빈 셀 0 | 유지 |
| 4 | M1 O1~O8 커버리지 | 8/8 | O1~O4=`01_`, O5~O7=`02_`, O8=`05_` 절 단위 존재 | 유지 |
| 5 | YAML 헤더 6필드 | 12/12 | `.md` **16/16 파일**이 `---`로 시작, 6필드 완비 | 유지 |
| 6 | ID 중복 | 0건 | `material_id` 179개 고유, `essential_id` 94개 고유, 중복 0 | 유지 |
| 7 | CSV 공란·스키마 결측 | 0셀 | 9개 CSV 필수필드 결측률 **0.0%**, 공란 0셀 | 유지 |
| 8 | R-13 E0·E1의 OMIT/SUBST/DOSE | 위반 0 | E0+E1 **41건 전수 보유**, 위반 0. REVIEW/VENDOR 단독 E0·E1 **0건** | 유지 |
| 9 | R-03 유예 표현 | 실질 0건 | 광역 정규식 15종 전 문서 스캔 → 후보 2건(`04_`:111 "나중에 세포를 얹는", `07_`:388 "향후 국제 경쟁력") 모두 기술·전망 서술, **실질 위반 0건**. 신규 삽입 문구("전문 확보 시 E1 재검토 대상")는 조건부 재검토 기록으로 유예 표현에 해당하지 않음 | 유지 |
| 10 | 날조 인용 | 0건 | 문서 고유 DOI **364건**, `evidence_ledger.md` 미등재 **0건**. HTML 고유 DOI **361건**도 전건 등재 | 유지 |
| 11 | 병합 정합성 | 78+74+33=185 | **78+71+30=179**, 차집합 0, 필드 단위 불일치 0 | 유지 |
| 12 | §4 금지 수치 | 무비판 인용 0 | 5개 금지 항목 전 문서 스캔 → 검출 7개소 **전부 반박·배제 문맥**(`02_`:227, `05_`:248, `08_`:458·460 등). 무비판 인용 0건 | 유지 |
| 13 | HTML 자체 완결성 | 외부 자원 0 | 외부 자원 참조 0건, 내부 앵커 **113개 전부 대상 존재**, `<figure>` 13 / 인라인 SVG 13 | 유지 |
| **14** | **헤더 `items_count` ↔ 데이터 행수** | **7/7 일치** | **5/7 일치.** `04_`=74 ↔ `inv_chem.csv`=**71**, `05_`=33 ↔ `inv_spec.csv`=**30** | **회귀** |

**회귀 1건 확인** — 병합(반려-004)이 CSV에만 적용되고 두 문서의 헤더·본문으로 전파되지 않았다.

---

## 3. 신규·잔존 문제 — 재반려 6건

### [재반려-101] 병합이 본문·헤더로 전파되지 않았다 (반려-004의 불완전 이행)

① **대상**: `05_specs_regulatory.md`:6(헤더)·14·22, `04_materials_chem.md`:6(헤더)·30

② **실측**:

| 위치 | 문서가 쓴 값 | 데이터 실측 |
|---|---|---|
| `05_`:6 `items_count` | **33** | `inv_spec.csv` **30행** |
| `05_`:14 | "등재 항목은 총 **33건**(L8 **16건**, L9 17건)이며 인벤토리는 `inv_spec.csv`에 있다" | L8 **13** / L9 17 = **30** |
| `05_`:22 | "아래 **16건**은 이런 '보이지 않는 변수'를…" | L8 **13건** |
| `04_`:6 `items_count` | **74** | `inv_chem.csv` **71행** |
| `04_`:30 | "등재 물질은 **74건**이며(L5 20건, L6 **29건**, L7 **25건**), **전량 `inv_chem.csv`에 동일 ID로 대응**" | L5 20 / L6 **27** / L7 **24** = **71**. 동일 ID 대응은 3건에서 성립하지 않음 |

③ **위반 규칙**: 게이트 G5(산출물 간 상호 정합성), 자기 데이터 서술 정확성.

④ **수정 방법**: `05_`는 헤더 `items_count`를 30, 14행을 "총 30건(L8 13건, L9 17건)", 22행을 "아래 13건"으로 고친다. `04_`는 헤더를 71, 30행을 "71건(L5 20건, L6 27건, L7 24건)"으로 고치고 "전량 동일 ID로 대응" 문구를 병합 3건 예외와 함께 적는다. `04_`는 10~24행에 병합 고지가 이미 있으나 그 고지가 건수 문장까지 덮지 못한다. `05_`에는 병합 고지 자체가 없다(grep 결과 0건).

---

### [재반려-102] `11_essential_materials.md` 본문 4개소가 자기 CSV와 어긋난다

① **대상**: `11_essential_materials.md`:289, 314, 320, 322

② **실측**:

| 위치 | 문서가 쓴 값 | `essential_items.csv` 실측 |
|---|---|---|
| :289 | "전체 94행 중 E0은 2행, **E1은 41행**이다" | **E1 39행**. 같은 문서 :378·:394는 "E0 2 / E1 **39** / E2 **44** / E3 9 — 내부 감사 반영 후 최종값"이라 적어 **문서 내부에서 자기모순** |
| :314 | **N-025**를 "### E1 조건부 필수 — 조성 물질 항목" 표에 등재 | N-025 = **E2**(본 재감사 반려-007 반영분). 하향 주석은 붙었으나 E1 표에서 빠지지 않음 |
| :320 | "**N-058~063** 세포 존재 하 겔화 화학"을 E1 표에 일괄 등재 | 6건 중 **N-058·N-062·N-063은 E2**, E1은 N-059·N-060·N-061 3건뿐 |
| :322 | "**N-072, N-074** 회수 경로(냉각 액화 / 효소 분해)"를 E1 표에 등재 | **N-072 = "Matrigel 내재 성장인자(정의성 위험)" E2, axis AX-6** — 회수 항목이 아님. **N-074 = "킬레이트·저온 기반 회수 용액" E2**. 실제 E1 회수 항목은 **N-073**(저온 가역 졸-겔 회수)·**N-075**(효소 회수) |

③ **위반 규칙**: R-13(등급 서술 정확성), G4·G5.

④ **수정 방법**: :289의 41 → 39. :314의 N-025 행을 E1 표에서 제거하고 §5 또는 변경 이력으로 옮긴다. :320의 "N-058~063"을 "N-059~061"로 좁힌다. :322의 "N-072, N-074"를 "N-073, N-075"로 고친다.

---

### [재반려-103] 신설된 `essentiality` 판정 기준이 자기 데이터 29행 중 18행에서 지켜지지 않는다 (반려-005의 불완전 이행)

① **대상**: `material_inventory.csv`의 `essentiality = 필수` 29행, `03_materials_bio.md`:139~148(신설 기준), `05_specs_regulatory.md`:76, HTML §8 표 3 · §9-2

② **실측**: 신설 기준은 "`essentiality` 열은 **오가노이드 3차원 지지체를 구성하는 매트릭스 소재로서의 필수도**를 뜻한다"고 정의하고, `선택`을 "**매트릭스 성분이 아니어서 매트릭스 필수도를 논할 대상이 아니다**"로 규정한다. 그런데 같은 하니스가 `05_`:76에서 "**L9는 물질이 아니라 시험 항목과 판정 기준의 집합이다**"라고 쓰고, HTML §8도 "**L9는 물질이 아니라 규격 항목이므로 품번이 없는 것이 정상**"이라고 쓴다.

- `필수` 29행의 층위 분포(HTML 표 3과 일치 확인): L1 3 · L5 4 · L6 3 · L7 2 · L8 3 · **L9 14**.
- **L9 14행**(내독소·생균수·무균시험·마이코플라스마·바이러스 안전성·동물유래성분/TSE·잔류 가교제·잔류 용매·세포독성 시험·무균화 방식·로트 간 G′ 편차·단백질 농도/순도·식별시험·안정성 및 유효기간)은 매트릭스 소재가 아니므로, **신설 기준에 따르면 전건 `선택`이어야 한다.**
- 이 값은 HTML **§9-2 표의 "필수도" 열에 `필수`로 그대로 출판**된다.

③ **위반 규칙**: R-13(근거 없는 "필수"), G2(등급 부여 근거 존재), G5(등급 체계 간 정합).

④ **수정 방법**: 두 경로 중 하나를 택한다. (ㄱ) L9 14행의 `essentiality`를 `선택`으로 내리고 규격 항목의 준수 의무는 별도 열 또는 `risk` 열로 표기한다. (ㄴ) `03_`의 기준 절에 "L9 규격 항목에 한해 `필수`는 '출하 판정 필수 시험 항목'을 뜻한다"는 **제3의 의미를 명시적으로 정의**하고, HTML §9-2 열 이름을 "필수도"에서 "규격 필수 여부"로 분리한다. 어느 쪽이든 `03_`의 기준 절과 데이터가 같은 뜻을 가리켜야 한다.

---

### [재반려-104] 인벤토리 `필수` ↔ M3 등급 상반이 4행 잔존 (반려-002의 동종 미해소)

① **대상**: `material_inventory.csv` C-L1-022, C-L1-034, C-L1-035, D-L6-016 (그리고 103에 포함된 E-L9-001)

② **실측**: 신설 기준표는 `필수` ↔ "E0 및 적용 조건이 넓은 E1", `조건부` ↔ "E1(조건 한정) 및 E2"로 매핑한다. 이 매핑을 데이터에 대면 다음이 어긋난다.

| 인벤토리 | `essentiality` | 대응 M3 항목 | `tier` | 기준상 기대값 |
|---|---|---|---|---|
| C-L1-022 라미닌-511(재조합 인간) | **필수** | N-007 라미닌-511 / 라미닌-521 (`tissue_scope` = 췌장) | **E2** | 조건부 또는 선택 |
| C-L1-034 기저막추출물(EHS, GFR) | **필수** | N-033 Matrigel 기준 물성값 / N-072 Matrigel 내재 성장인자 / N-084 GFR 등급 | **전건 E2** | 조건부 |
| C-L1-035 기저막추출물(Cultrex RGF BME Type 2) | **필수** | 동일 | **전건 E2** | 조건부 |
| D-L6-016 광개시제 LAP | **필수** | N-062 광가교(GelMA + LAP, 405/365 nm) (`tissue_scope` = 폐; 뼈·연골) | **E2** | 조건부 또는 선택 |
| E-L9-001 내독소 | **필수** | N-082 내독소 상한 규격 (`tissue_scope` = 전체) | **E3** | 선택 |

**참고로 라미닌은 방향까지 뒤집혀 있다.** M3에서 E0 판정을 받은 N-002는 "α6β1 인테그린 리간드(**라미닌-111** 결합면)"이고 라미닌-111은 N-003·N-004·N-005로 E1 3건을 더 갖는데, 인벤토리에서 라미닌-111(C-L1-014·C-L1-016)은 **`조건부`**이고 E2 근거밖에 없는 라미닌-511이 **`필수`**다. `11_`:§5-1도 "라미닌**-111**에 대해서는 간·뇌·…"라고 적어 본문 논지와도 반대다.

③ **위반 규칙**: R-13, G2, G5(동일 물질 필수도 일치).

④ **수정 방법**: 위 표의 5행을 M3 등급에 맞춰 조정하거나, 조정하지 않는다면 각 행의 `confidence_reason`에 "M3 등급 E2/E3에도 불구하고 `필수`를 유지하는 근거"를 실험 근거와 함께 기재한다. 라미닌 건은 C-L1-014·C-L1-016(라미닌-111)과 C-L1-022(라미닌-511)의 값을 M3 판정 순서대로 맞춘다. 아울러 `D-L5-013`(N-050 E1, 췌장·장 한정), `D-L6-021`·`D-L6-022`·`D-L7-001`·`D-L7-005`(N-064 E1, 장·췌장·뇌 한정) 5행도 "조건 한정 E1 → `조건부`" 규칙에 해당하므로 함께 점검 대상이다.

---

### [재반려-105] 초록 전용 근거의 E1이 2건 잔존하고, 지시된 사유 특정이 이행되지 않았다 (반려-007의 부분 이행)

① **대상**: `essential_items.csv` N-045, N-087

② **실측**: `experiment_summary`·`confidence_reason`·`open_issues`를 "본문 미확인 / 초록 범위" 정규식으로 전수 스캔한 결과 **8행**이 걸렸고, 그중 **E1은 N-045·N-087 2행**이다(나머지는 E2 5행·E3 1행).

- N-045: `tier` E1, `confidence` 중, `quant_range` = "**정량 근거 없음(초록 범위)** + 신장 0.1/3/20 kPa 비교"
- N-087: `tier` E1, `confidence` 중

1차 감사의 수정 방법 ④는 "N-045·N-087은 초록 근거 **외에 전문 확인 근거를 함께 보유**하므로 현 등급을 유지하되, `confidence_reason`에 **어느 근거가 등급을 지탱하는지 명시**한다"였다. 실제 기재된 문구는 두 행 모두 **"근거 혼합 — 일부는 전문에서 실험 조건 직접 확인, 일부는 초록 범위에 그침"**으로, 어느 DOI가 E1을 지탱하는지 특정하지 않는다. 지시의 문면은 이행되지 않았다.

③ **위반 규칙**: R-13, R-07, G4(E0·E1 근거의 실질성).

④ **수정 방법**: 두 행의 `confidence_reason`에 E1을 지탱하는 문헌을 DOI 단위로 명기한다(예: "E1 판정은 전문을 확인한 DOI:… 의 OMIT 결과가 지탱하며, 초록 범위 근거인 DOI:… 는 보조"). 전문 확인 근거를 특정할 수 없다면 N-025·N-043과 같은 사유로 E2로 내린다.

---

### [재반려-106] `validate.py` 신설 검사에 위양성 1건·구조적 사각 4종 (반려-009의 부분 이행)

4절에 시험 방법과 결과를 전부 기재한다. 요지는 다음 둘이다.

- **위양성**: `[R-13] essentiality 판정 기준이 문서에 정의됨` 검사는 "어느 `.md`에든 `essentiality`와 `판정 기준`이 함께 나오면" 통과한다. `03_materials_bio.md`의 기준 절을 **통째로 삭제한 사본도 PASS**했고, 검사를 충족시킨 파일은 `09_audit.md`(기준 부재를 지적한 감사문 자신)와 `orchestrator_log.md`였다. **기준을 지적한 문서가 기준의 존재를 증명하는 구조**다.
- **구조적 사각**: `[G4] tier ↔ 매트릭스` 검사는 "항목 E0 ↔ 해당 축 전 열 E3"이라는 **최대 모순만** 결함으로 본다. 1차 반려-006이 지적한 3셀은 E0↔E3형이 아니었으므로 이 검사로는 원래 잡히지 않는다. `[R-13] 초록 전용` 검사는 `confidence == '하'`를 AND 조건으로 걸어, 잔존 위반인 N-045·N-087(둘 다 `중`)을 구조적으로 탐지할 수 없다. `BAD_MISSING`에 `미확인`이 없어 재반려-107 대상 2셀을 놓친다. 헤더 `items_count` ↔ 데이터 행수 검사는 코드에 존재하지 않는다(`grep -c items_count tools/validate.py` = **0**).

④ **수정 방법**: (ㄱ) 기준 문서화 검사의 탐색 대상을 `03_materials_bio.md` 한 파일로 고정하고, 기준표의 3개 값(`필수`·`조건부`·`선택`)이 모두 등장하는지까지 확인한다. (ㄴ) tier↔매트릭스 검사에서 `E0·E1` 항목 ↔ 해당 축 `E3` 셀을 전부 보고 대상으로 넓힌다. (ㄷ) 초록 전용 검사에서 `confidence == '하'` AND 조건을 제거하고 `tier in (E0,E1)` AND "본문 미확인" 만으로 판정한다. (ㄹ) `BAD_MISSING`에 `미확인`·`확인불가`·`없음`을 추가한다. (ㅁ) 각 `.md` 헤더의 `items_count`를 대응 CSV 행수와 대조하는 검사를 신설한다. (ㅂ) `essentiality`(필수/조건부/선택) ↔ `essential_items.tier`(E0~E3) 교차 검사를 신설한다 — 재반려-104는 이 검사가 있었다면 자동 탐지된다.

---

### [재반려-107] 사유 없는 결측 표지 2셀 잔존 (반려-008의 동종 미해소)

① **대상**: `patents_landscape.csv` US20070224273A1의 `assignee`, `assignee_type`

② **실측**: 9개 CSV 전 셀을 `unknown / 미상 / n/a / - / ? / 없음 / 미확인 / 미정` 표지로 스캔한 결과 **3셀**이 걸렸다.

| 파일 | ID | 열 | 값 |
|---|---|---|---|
| `patents_landscape.csv` | US20070224273A1 | `assignee` | **미확인** |
| `patents_landscape.csv` | US20070224273A1 | `assignee_type` | **미확인** |
| `essential_items.csv` | N-052 | `alternatives` | 없음 |

앞 2셀은 `미상`과 동일하게 사유·확인 경로·날짜가 없다(`04_`:173이 선언한 "결측은 `unknown`이 아니라 사유를 명시" 규칙 위반). N-052의 `alternatives` = "없음"은 "대체재가 존재하지 않는다"는 **실질 판정**으로 읽히므로 결함으로 보지 않는다.

③ **위반 규칙**: 스키마 결측 규칙, G2.

④ **수정 방법**: 2셀을 "확인 시도 경로 + 날짜"가 포함된 사유 문자열로 교체한다(반려-008에 적용한 것과 동일한 형식). 동시에 `validate.py`의 `BAD_MISSING`에 `미확인`을 추가한다.

---

### 3-8. 재반려에 이르지 않는 경미 사항 2건

- **`figures/figure_specs.md`:88**에 합계 **185**, **`orchestrator_log.md`:38**에 "총 **185건** ≥ MIN_MATERIALS(80)", **`10_confidence.md`:221**에 "물질 인벤토리 (**185건**)"가 남아 있다. 다만 `orchestrator_log.md`:110이 "185 → **179건**" 병합을 기록하고 `10_confidence.md`:12~18이 "본 판정은 감사 반영 **이전** 스냅숏"임을 고지하므로, 독자가 오인할 경로는 차단되어 있다. 세 파일 모두 본 재감사의 지정 검사 대상(`01_`~`07_`, `11_`, `12_`)이 아니다.
- **HTML §6-2** 셋째 문단에 "이 행이 **매트릭스 매트릭스에서** E3로 나오는 것은"이라는 어구 중복이 있다. 내용상 오류는 아니다.

---

## 4. `validate.py` 탐지력 시험

**방법**: 원본을 수정하지 않고 `/tmp` 사본 트리를 만들어 위반을 한 종류씩 주입한 뒤 `python3 tools/validate.py <사본>`을 돌려 `FAIL` 행 출력 여부를 확인했다. 기준선(무주입 사본)은 **PASS · 오류 0건**이다.

| # | 주입한 위반 | 대응 반려 | 결과 |
|---|---|---|---|
| T1 | C-L4-001(표피성장인자) `essentiality`를 `필수`로 되돌림 | 001 | **탐지** — `FAIL [R-10] 배지·비매트릭스 성분에 '필수' 부여: C-L4-001 표피성장인자` |
| T2 | C-L2-005를 복제해 `D-L6-020`/`L6`로 재등재 | 004 | **탐지** — `FAIL [G2] 동일 물질이 서로 다른 층위에 중복 등재: '메타크릴레이트화히알루론산' → [('L2','C-L2-005'),('L6','D-L6-020')]` |
| T3 | `03_materials_bio.md`의 기준 절 전체 삭제 | 005a | **미탐지** — PASS. 검사를 충족시킨 파일은 `09_audit.md`·`orchestrator_log.md` |
| T4 | C-L1-014(라미닌-111 천연)만 `필수`로 변경 → SKU 역전 | 005b | **탐지** — `FAIL [R-13] 동일 물질의 제품 간 필수도 불일치: '라미닌111' → ['조건부','필수']` |
| T5 | `patents_landscape.csv` 첫 행 `ipc`를 `미상`으로 되돌림 | 008 | **탐지** — `FAIL [특허랜드스케이프] 사유 없는 결측 표지 '미상': US4829000A / ipc` |
| T6 | N-043을 `E0` + `axis=AX-4` + `tissue_scope=간`으로 변경 | 006 | **탐지** — `FAIL [G4] … 간/AX-4: 항목 N-043 E0 ↔ 매트릭스 전 열 E3` (+ 초록 전용 FAIL 동시 발생) |
| T7 | N-025를 `E1`로 되돌림 | 007 | **탐지** — `FAIL [R-13] 초록 전용·신뢰도 하 근거로 E1 부여: N-025` |
| T8 | `essentiality`에 영역 외 값 `매우필수` 주입 | 005 | **탐지** — `FAIL [M2] essentiality 값 오류: C-L1-001 = '매우필수'` |
| T9 | (무주입) 현존 N-045·N-087 — 초록 전용 E1, `confidence` 중 | 007 잔존 | **미탐지** — `confidence == '하'` AND 조건 때문에 구조적으로 탐지 불가 |
| T10 | (무주입) 현존 `04_` 74≠71, `05_` 33≠30 | 재반려-101 | **미탐지** — 검사 항목 자체가 없음(`grep -c items_count` = 0) |
| T11 | `patents_landscape.csv`에 `미확인` 주입 | 재반려-107 | **미탐지** — `BAD_MISSING`에 `미확인` 부재 |
| T12 | (무주입) 현존 E-L9-001 `필수` ↔ N-082 `E3` | 재반려-104 | **미탐지** — `essentiality` ↔ `tier` 교차 검사 코드 부재 |

**결과: 12종 중 8종 탐지(66.7%), 4종 미탐지.** 1차에서 도구는 반려 8건을 전부 놓쳤으므로 탐지력은 실질적으로 개선되었다. 다만 T3는 **검사가 있으나 위양성**이라는 점에서 미탐지보다 위험하다 — 기준 절이 없어도 PASS가 나오므로 도구의 PASS를 근거로 기준 존재를 주장할 수 없다.

---

## 5. 게이트 재판정

### G2 — M2 물질 인벤토리 · **실패** (1차 실패 → 유지)

| 기준 | 실측 | 판정 |
|---|---|---|
| L1~L9 전 층위 등재 | 9/9, 최소 L3=10건 | 충족 |
| 최소 물질 수 80건 | **179건 (224%)** | 충족 |
| 필수필드 결측률 < 20% | **0.0%** (공란 0셀) | 충족 |
| `material_id` 중복 0 | 0건 | 충족 |
| L1·L6·L7 공급사+품번 보유율 ≥ 60% | **97.7% (84/86)** | 충족 |
| 물질 단위 무결성(동일 물질 1행) | 층위 간 중복 **6군 → 0군** | **충족(해소)** |
| 역할 기재 정확성(R-10) | 배지·공정 성분 `필수` **6행 → 0행** | **충족(해소)** |
| **등급 부여 근거 존재·일관 적용** | 기준 절은 신설됨. 그러나 `필수` **29행 중 18행**(L9 14 + E2/E3 대응 4)이 그 기준과 어긋남 | **미충족([재반려-103]·[재반려-104])** |
| **본문·헤더 ↔ 데이터 건수 정합** | `04_` 74≠71, `05_` 33≠30, 본문 3개소 추가 불일치 | **미충족([재반려-101])** |
| **결측 표지 규칙** | 사유 없는 표지 **3셀 → 2셀** | **미충족([재반려-107])** |

**판정: 실패.** 1차의 미충족 3개(반려-001·004·005) 중 **2개는 해소**되었다. 정량 기준 5개는 여유 있게 충족한다. 그러나 반려-005는 "기준을 문서에 쓴다"까지만 이행되고 "그 기준을 데이터에 적용한다"가 이행되지 않아, 기준이 생긴 결과 **위반이 측정 가능해졌을 뿐 줄지는 않았다**(29행 중 18행, 62%). 여기에 병합 미전파와 결측 표지 잔존이 더해져 미충족 항목은 3개다.

### G4 — M3 필수물질 판정 · **실패** (1차 조건부 통과 → 하향)

| 기준 | 실측 | 판정 |
|---|---|---|
| AX-1~AX-9 전 축 탐색 | 9/9 (최소 AX-6·AX-7 각 9건) | 충족 |
| ORGANOID_TYPES 8조직 행 | 8/8, **80셀 전건 채움** | 충족 |
| 매트릭스 셀 값 E0~E3 규격 | 규격 외 0건 (E1 23 · E2 33 · E3 24) | 충족 |
| E0·E1의 OMIT/SUBST/DOSE 보유 | **41/41 보유**, 위반 0건 | 충족 |
| REVIEW/VENDOR 단독 E0·E1 | 0건 (REVIEW 12건 중 E0·E1은 전부 SUBST·DOSE 병기) | 충족 |
| `tier`·`evidence_type`·`confidence` 값 영역 | 위반 0건 | 충족 |
| `confidence_reason` 충실성 | 5자 미만 0건 | 충족 |
| 항목 등급 ↔ 매트릭스 셀 일치 | 지적 3셀 전건 수정 확인, 최대 모순 0건 | **충족(해소)** |
| **E0·E1 근거의 실질성** | 초록 전용 E1 **4건 → 2건**. 잔존 2건(N-045·N-087)의 사유 특정 지시 미이행 | **미충족([재반려-105])** |
| **본문(`11_`) 등급 서술 ↔ CSV** | **4개소 불일치**(:289 E1 41행, :314 E2 항목이 E1 표에, :320 6건 중 3건만 E1, :322 ID 오기) | **미충족([재반려-102])** |

**판정: 실패.** 1차에서 G4를 통과시킨 근거는 "불일치 5건은 국소적이며 [반려-006]·[반려-007] 수정으로 해소 가능하므로 **수정을 전제로** 통과"였다. 반려-006은 완전히 이행되었으나 반려-007은 4건 중 2건만 이행되었고, 이행하지 않은 2건에 대해 지시된 대체 조치(사유 특정)도 수행되지 않았다. **통과의 전제가 충족되지 않았으므로 조건부 통과를 유지할 수 없다.** 여기에 판정 문서 본문의 등급 서술 불일치 4개소가 새로 확인되었다.

### G5 — 산출물 간 상호 정합성 · **실패** (1차 실패 → 유지)

> G5의 정의는 1차와 동일하게 "12개 산출물이 동일 사실에 대해 상충하는 값을 발행하지 않을 것"으로 둔다.

| 기준 | 실측 | 판정 |
|---|---|---|
| 유예 표현(R-03) 0건 | 후보 2건 전부 기술·전망 서술, 실질 위반 0건 | 충족 |
| §4 금지 수치 무비판 인용 0건 | 검출 7개소 전부 반박·배제 문맥 | 충족 |
| 날조 인용 0건 | 문서 고유 DOI 364 / HTML 361, 대장 미등재 **0건** | 충족 |
| 병합 정합성 | 78+71+30=179, 차집합 0, 필드 불일치 0 | 충족 |
| R-10 2D↔3D · 체외↔체내 구분 | 위반 0건 (HTML "계속 구분하는 세 쌍" 포함) | 충족 |
| R-10 배지↔매트릭스 구분 | 위반 **6행 → 0행**, HTML 부록 A에도 사유와 함께 반영 | **충족(해소)** |
| `11_`↔`12_` 필수성 개념 분리 | `CLAIM` 등급 **0건** 실측, `patent_ref` 94/94행 사유 문자열, `11_`:60 서술 일치 | **충족(해소)** |
| HTML 집계 ↔ CSV | E0 2·E1 39·E2 44·E3 9, 179건, 527/26/4.9%, 40·31·85·31/116 **전건 일치** | **충족(해소, X-001)** |
| 판정 파일 간 정합(tier↔매트릭스) | 지적 3셀 수정 확인, 최대 모순 0건 | **충족(해소)** |
| **자기 데이터 서술 정확성** | `11_`:60 해소. **`11_`:289·314·320·322, `04_`:30, `05_`:14·22 6개소 불일치** | **미충족([재반려-101]·[102])** |
| **헤더 `items_count` ↔ 데이터 행수** | **5/7** (1차 7/7에서 회귀) | **미충족([재반려-101])** |
| **동일 물질 필수도 일치** | 배지 6건 해소. **인벤토리 `필수` ↔ M3 E2/E3 상반 5행 잔존** | **미충족([재반려-104])** |
| **등급 체계 간 정합** | `essentiality`↔E0~E3 매핑표 신설(해소). **매핑 위반 18행** | **미충족([재반려-103])** |
| **결측 표지 규칙** | `미상` 3셀 해소, `미확인` **2셀 잔존** | **미충족([재반려-107])** |
| **검사 체계 신뢰성** | 주입 12종 중 8종 탐지. **위양성 1 + 사각 4** | **미충족([재반려-106])** |

**판정: 실패.** 15개 기준 중 **9개 충족 · 6개 미충족**이다. 1차와 미충족 수는 같으나 **구성이 완전히 달라졌다** — 1차의 6개 미충족(R-10 배지↔매트릭스, 동일 물질 필수도, 자기 데이터 서술, 등급 체계, tier↔매트릭스, 검사 체계) 중 **4개가 해소**되었고, 대신 **병합·하향이 CSV에만 적용되고 본문으로 전파되지 않아 생긴 새 유형 2개**(건수 정합, 헤더 `items_count`)가 들어왔다.

1차 실패의 핵심은 "R-10을 가장 잘 아는 하니스가 자기 인벤토리에서 R-10을 어겼다"였고, 그 문제는 해소되었다. **2차 실패의 핵심은 단일하다 — 수정이 데이터 계층에서 멈췄다.** CSV 6행 하향·6군 병합·3셀 교체·2행 하향은 전부 정확히 수행되었고 HTML 재생성도 정확했다. 전파되지 않은 곳은 **사람이 손으로 쓴 산문**이다(`04_`:30, `05_`:14·22, `11_`:289·314·320·322). 정정 대상은 본문 7개 문장 + 헤더 2개 필드 + CSV 최대 20행 + `patents_landscape.csv` 2셀 + `validate.py` 6개 규칙이며, 산출물의 구조나 근거 기반을 다시 만들 필요는 없다. 근거 무결성(날조 인용 0건, DOI 364건 전건 대장 등재)은 1차와 동일하게 유지된다.

**게이트 재판정: G2 실패 · G4 실패 · G5 실패.**

---

## 6. 남은 미해결 항목 (6건)

| # | 재반려 | 대상 파일 | 위반 규칙 | 관련 1차 반려 |
|---|---|---|---|---|
| 1 | [재반려-101] 병합이 본문·헤더 미전파 (5개소) | `04_`:6·30, `05_`:6·14·22 | G5, 자기 데이터 서술 | 004 |
| 2 | [재반려-102] `11_` 본문 4개소 ↔ CSV 불일치 | `11_`:289·314·320·322 | R-13, G4, G5 | 007(파생) |
| 3 | [재반려-103] 신설 기준 미적용 (`필수` 29행 중 18행) | `material_inventory.csv`, `03_`:139~148, HTML §9-2 | R-13, G2, G5 | 005 |
| 4 | [재반려-104] 인벤토리 `필수` ↔ M3 등급 상반 5행 | `material_inventory.csv` C-L1-022·034·035, D-L6-016, E-L9-001 | R-13, G2, G5 | 002 |
| 5 | [재반려-105] 초록 전용 E1 2건 + 사유 미특정 | `essential_items.csv` N-045·N-087 | R-13, R-07, G4 | 007 |
| 6 | [재반려-106] `validate.py` 위양성 1 + 사각 4 | `tools/validate.py` | 검사 체계 신뢰성, G5 | 009 |

> [재반려-107]은 [재반려-106]의 `BAD_MISSING` 보강과 동시에 처리되는 2셀 수정이므로 위 6건에 포함해 센다(4번 항목과 함께 `patents_landscape.csv` 2셀로 처리).

수정 권한은 각 산출물의 담당 에이전트에게 있으며, 본 감사는 원문을 수정하지 않았다.

---

## 7. 1차 감사 요약 (압축)

**1차 감사일 2026-09-14 · 검사 28항목 중 통과 19 · 반려 9 · 게이트 G2 실패 · G4 조건부 통과 · G5 실패.**

통과 19항목은 (ㄱ) 누락 축 — L1~L9 층위 185건, AX-1~AX-9 187회, 8조직 80셀, M1 O1~O8, YAML 헤더 12/12, (ㄴ) 스키마 축 — CSV 공란 0셀, `confidence` 값 영역 위반 0, `confidence_reason` 5자 미만 0, `tier`·`evidence_type` 영역 위반 0, ID 중복 0, (ㄷ) 규칙 축 — R-10 2D↔3D·체외↔체내 구분 위반 0, R-13 E0·E1의 강근거 보유 43/43, REVIEW/VENDOR 단독 E0·E1 0건, R-03 실질 위반 0, §4 금지 수치 무비판 인용 0, (ㄹ) 근거 축 — 고유 DOI 364건 전건 대장 등재(날조 0), 병합 정합 78+74+33=185, 헤더 `items_count` 7/7, (ㅁ) 물성 수치 상충이 미해결 쟁점으로 명시 처리됨 — 이었다.

반려 9건은 다음과 같다.

| 반려 | 요지 | 대상 |
|---|---|---|
| 001 | 배지·공정 성분 6행에 매트릭스 `필수` 부여 (R-10) | C-L4-001/002/003/012/013, E-L8-004 |
| 002 | 동일 물질 필수도가 4개 산출물 간 상반 (인벤토리 `필수` ↔ M3 E3) | 배지 성장인자 6건 |
| 003 | `11_`:60이 `patent_ref` 열 상태를 사실과 다르게 서술 | `11_essential_materials.md`:60 |
| 004 | 동일 물질이 다른 ID로 중복 등재 6군 (HAMA·티올화HA·GelMA·CaCl₂·GDL·BaCl₂) | `material_inventory.csv` |
| 005 | `essentiality` 3단계의 판정 기준이 12개 문서 어디에도 정의되지 않음 (grep 0건), SKU별 등급 상충 8군 | 전 인벤토리 |
| 006 | 항목 등급 ↔ 매트릭스 셀 3개 셀·5개 항목 불일치 | 간/AX-1, 장/AX-4, N-094 `axis` |
| 007 | 초록 전용 근거로 E1 부여 4건, 그중 신뢰도 `하` 2건 | N-025·N-043·N-045·N-087 |
| 008 | 사유 없는 `미상` 3셀 | `patents_landscape.csv` |
| 009 | `validate.py`가 위 반려 8건을 전부 미탐지 (검사 범위 결손 7항목) | `tools/validate.py` |

1차 실패 진단의 요지는 "하니스가 R-10을 가장 정확히 서술하는 여러 곳(`01_`:55, `11_`:202~208, 매트릭스 AX-6 8조직 전부, HTML 본문)을 두고도 자기 인벤토리에서 그 규칙을 어겼다"는 것이었다. 재감사 결과 그 지적은 해소되었다.

---

## 8. 재감사 종료 선언

재감사 항목 37건을 검사했다. 1차 반려 9건 + 레드팀 지적 3건 = **12건 중 완전 이행 6건, 부분 이행 6건, 미이행 0건**. 신규·잔존 문제로 **재반려 6건**(경미 2건 별도)을 제기한다. 1차 통과 14개 축 중 **13개 유지, 1개 회귀**(헤더 `items_count` 7/7 → 5/7).

본 감사는 원문 데이터를 수정하지 않았다. `validate.py` 탐지력 시험은 전부 `/tmp` 사본에서 수행했고 원본 도구와 데이터는 변경되지 않았다.

**게이트 재판정: G2 실패 · G4 실패(1차 조건부 통과에서 하향) · G5 실패.**
